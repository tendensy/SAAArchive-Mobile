These are the latest RSA_PUB_KEY and TEA_XOR_KEY exported from the 0.3.7-R4-2 client.

These keys will not work with the currently available archive file in the build directory,
and cannot generate a new archive file without the RSA private key.
